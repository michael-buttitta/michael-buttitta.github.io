# Bundler cache
.bundle
vendor
Gemfile.lock

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json

# Local editor's cached copies of the deployed page shell
tools/.shell-cache/

# Front Matter CMS local state (the shared config, frontmatter.json, IS tracked)
.frontmatter/

# Claude Code local session state (.claude/commands/ IS tracked)
.claude/worktrees/

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/vendors
assets/js/dist

# Internal docs — kept on disk locally, never tracked or published
CLAUDE.md
BUILD-NOTES.md
CONTENT-PLAYBOOK.md
_audit/
